import { createStore } from "vuex";
import dashStore from "@/resources/_store";

export default createStore({
    state: {},
    getters: {},
    mutations: {},
    actions: {},
    modules: { dashStore },
});
